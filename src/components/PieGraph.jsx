import Chart from "react-google-charts";

export default function PieGraph({ data, options }) {
  return (
    <Chart
      chartType="PieChart"
      data={data}
      options={options}
      width={"100%"}
      height={"400px"}
    />
  );
}
